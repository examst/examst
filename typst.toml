[package]
name = "examst"
version = "0.0.1"
entrypoint = "src/lib.typ"
authors = ["Andrew Liu <https://landrew.dev>"]
license = "MIT"
description = "Utilities for exam-writing"
exclude = [ "/docs/*", "/example/*" ]
